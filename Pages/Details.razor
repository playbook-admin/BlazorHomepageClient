@page "/details/{photoId}/{albumId}/{albumCaption}"
@code {
    [Parameter] public string photoId { get; set; } = string.Empty;
    [Parameter] public string albumId { get; set; } = string.Empty;
    [Parameter] public string albumCaption { get; set; } = string.Empty;
}
<h3>Details of Photo @photoId</h3>
<!-- Details component implementation -->
